{

  "apps" : [
    {
      "name"      : "jsonlint",
      "script"    : "index.js",
      "env": {},
      "env_production" : {
        "NODE_ENV": "production",
        "PORT": "5001",
        "API_ONLY": "yes"
      }
    }
  ],

  "deploy" : {
    "production" : {
      "user" : "deployer",
      "host" : "192.241.157.86",
      "ref"  : "origin/master",
      "repo" : "git@github.com:circlecell/jsonlint.git",
      "path" : "/var/web/beta.jsonlint.com",
      "ssh_options": ["StrictHostKeyChecking=no", "PasswordAuthentication=no"],
      "post-deploy" : "rm -rf node_modules; rm -rf frontend/node_modules; npm install && npm run npm-frontend -- install && npm run bundle && npm run serve-prod"
    },
  }
}
